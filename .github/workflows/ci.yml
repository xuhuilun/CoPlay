name: CI

on:
  push:
    branches:
      - main
      - "feature/**"
  pull_request:
    branches:
      - main

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npm run prisma:generate -w apps/api

      - name: Test
        run: npm test

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

      # Fail the build on high/critical production advisories. Two moderate react-router
      # advisories currently have no advisory-free release (see docs/security.md); they are
      # non-applicable to this client-only SPA, so the gate is set at high rather than ignored.
      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high
